package org.ferdidrgn.hudaquran.ui.theme

import androidx.compose.ui.graphics.Color

// Dark mode — a deep green-black canvas instead of a generic neutral-gray "SaaS dark mode,"
// matching the same illuminated-manuscript identity as the Mushaf page border and onboarding.
val Canvas = Color(0xFF0A140F)
val SurfaceDark = Color(0xFF14231C)
val SurfaceHighlight = Color(0xFF1E3227)
val BorderDark = Color(0x14FFFFFF) // white 8%
val BorderDarkStrong = Color(0x24FFFFFF) // white 14%
val TextPrimaryDark = Color(0xFFF3EEDF) // warm cream, not stark white
val TextSecondaryDark = Color(0xFFA9B3A6)

// Light mode — warm neutral canvas
val CanvasLight = Color(0xFFF8F6F0)
val SurfaceLightMode = Color(0xFFFFFFFF)
val SurfaceHighlightLight = Color(0xFFF1EEE4)
val BorderLight = Color(0x14000000)
val TextPrimaryLight = Color(0xFF1C241F)
val TextSecondaryLight = Color(0xFF6C7669)

// Accents — a gilt gold (the same tone the Mushaf page border and onboarding CTA use) paired
// with the app's Islamic green, replacing the earlier generic indigo/purple accent.
val Gilt = Color(0xFFD4B36A)
val GiltBright = Color(0xFFE7CC8F)
val Emerald = Color(0xFF1F9E6C)
val EmeraldBright = Color(0xFF44C393)
val Amber = Color(0xFFE0A840)
val Rose = Color(0xFFD8677B)

// Splash — the app's original deep religious green, kept independent of the accent theme
val SplashGreenDark = Color(0xFF0F4A2C)
val SplashGreen = Color(0xFF1E7145)
val SplashGold = Color(0xFFC9A227)
